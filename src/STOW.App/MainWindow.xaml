<Window x:Class="STOW.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="STOW"
        Width="1180" Height="760"
        MinWidth="960" MinHeight="640"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource CanvasBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="232" />
            <ColumnDefinition Width="1" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}">
            <Grid Margin="10,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Margin="14,10,14,24">
                    <TextBlock Text="STOW" FontSize="{DynamicResource FontSize22}" FontWeight="SemiBold" />
                    <TextBlock Text="A calmer desktop starts here." Margin="0,5,0,0"
                               FontSize="{DynamicResource FontSize115}"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource TextSecondaryBrush}" />
                </StackPanel>
                <StackPanel Grid.Row="1">
                    <Button x:Name="AppsNavButton" Style="{StaticResource NavButton}" Content="_Apps" Tag="Apps" Click="Navigate_Click"
                            Background="{DynamicResource SelectionBrush}" Foreground="{DynamicResource SelectionForegroundBrush}" />
                    <Button Style="{StaticResource NavButton}" Content="_Rules" Tag="Rules" Click="Navigate_Click" />
                    <Button Style="{StaticResource NavButton}" Content="_Focus" Tag="Focus" Click="Navigate_Click" />
                    <Button Style="{StaticResource NavButton}" Content="_Insights" Tag="Insights" Click="Navigate_Click" />
                    <Button Style="{StaticResource NavButton}" Content="_Settings" Tag="Settings" Click="Navigate_Click" />
                </StackPanel>

                <Button Grid.Row="3" Style="{StaticResource NavButton}" Content="A_bout" Tag="About"
                        Click="Navigate_Click" Margin="8,8,8,2" />
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="{DynamicResource BorderBrush}" />

        <Grid Grid.Column="2" Margin="40,34,44,40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="28" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock x:Name="PageTitleText"
                           Text="Apps"
                           Style="{StaticResource PageTitle}"
                           AutomationProperties.LiveSetting="Polite" />
                <TextBlock x:Name="PageSubtitleText" Text="Choose what STOW keeps quietly out of your way."
                           Style="{StaticResource PageSubtitle}" />
            </StackPanel>

            <ContentControl x:Name="PageHost" Grid.Row="2" />
        </Grid>
    </Grid>
</Window>
